'''
the line_eval function scores every item passed in
takes in a list of requirements (string) and a list of items (Item class)
currently, no cache is used, could be a function for the future
'''

def line_eval(requirements: list, items: list) -> bool:
    """
    Evaluates each item against the requirements and returns True if all items meet the requirements.
    
    side effect: modify each Item in items to contain the calculated score
    return: bool to indicate whether the process is successful
    """
    return True

